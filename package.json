{
  "name": "eisenhower-mcp",
  "version": "1.0.0",
  "description": "MCP server for Eisenhower matrix task classification",
  "scripts": {
    "start": "python -m src",
    "dev": "python -m src",
    "test": "pytest tests/",
    "lint": "ruff check src/ tests/",
    "format": "black src/ tests/ && isort src/ tests/ && autoflake --in-place --recursive --remove-all-unused-imports --remove-unused-variables src/ tests/",
    "typecheck": "mypy src/ tests/"
  },
  "keywords": [
    "mcp",
    "eisenhower",
    "todoist",
    "productivity",
    "task-management"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}